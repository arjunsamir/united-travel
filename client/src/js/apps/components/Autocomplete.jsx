import React from 'react';

import MaterialAutoComplete, { createFilterOptions } from '@material-ui/lab/Autocomplete';

// Create Component
const Autocomplete = ({ id, options }) => {
    return (
        <div></div>
        // <MaterialAutoComplete
        //     id={id}
        //     options={}
        // />
    );
};

export default Autocomplete;
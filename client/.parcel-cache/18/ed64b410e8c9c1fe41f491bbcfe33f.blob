"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
exports["default"] = getCurrentMonth;
var _helpers = require("../../Calendar/helpers");
function getCurrentMonth(currentMonth, minDate, maxDate) {
    var month = _helpers.getFirstDayMonth(_helpers.normalizeDate(currentMonth));
    month.setHours(0, 0, 0, 0);
    if (minDate) {
        var minMonth = _helpers.getFirstDayMonth(_helpers.normalizeDate(minDate));
        minMonth.setHours(0, 0, 0, 0);
        if (_helpers.isDateBelowLimit(month, minMonth)) return minMonth;
    }
    if (maxDate) {
        var maxMonth = _helpers.getFirstDayMonth(_helpers.normalizeDate(maxDate));
        maxMonth.setHours(0, 0, 0, 0);
        if (_helpers.isDateBeyondLimit(month, maxMonth)) return maxMonth;
    }
    return month;
}
